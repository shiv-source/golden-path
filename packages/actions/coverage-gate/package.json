{
    "name": "@golden-path/action-coverage-gate",
    "version": "0.0.0",
    "private": true,
    "type": "module",
    "description": "TypeScript action: run Go tests and enforce a coverage floor.",
    "scripts": {
        "typecheck": "tsc -p tsconfig.json --noEmit",
        "test": "vitest run"
    },
    "dependencies": {
        "@actions/core": "3.0.1",
        "@golden-path/core": "workspace:*"
    }
}
