{
    "name": "@golden-path/action-parse-config",
    "version": "0.0.0",
    "private": true,
    "type": "module",
    "description": "TypeScript action: parse .github/golden-path.yml into normalized outputs.",
    "scripts": {
        "typecheck": "tsc -p tsconfig.json --noEmit",
        "test": "vitest run"
    },
    "dependencies": {
        "@actions/core": "3.0.1",
        "@golden-path/core": "workspace:*"
    }
}
