{
    "name": "@golden-path/action-issue-labels",
    "version": "0.0.0",
    "private": true,
    "type": "module",
    "description": "TypeScript action: apply three-tier issue labels (type, priority, areas) from issue-form answers.",
    "scripts": {
        "typecheck": "tsc -p tsconfig.json --noEmit",
        "test": "vitest run"
    },
    "dependencies": {
        "@actions/core": "3.0.1"
    }
}
